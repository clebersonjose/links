version: '3.9'

services:

  db:
    image: postgres
    restart: always
    shm_size: 128mb
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: cc077383d01718bce6f2aebbefa1710e
      POSTGRES_USER: cj
      POSTGRES_DB: cj_links
